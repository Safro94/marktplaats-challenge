import CarFormContainer from '../../containers/carFormContainer';

import { Wrapper } from './index.styles';

const Home = () => {
	return (
		<Wrapper>
			<CarFormContainer />
		</Wrapper>
	);
};

export default Home;
